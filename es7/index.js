// Se agrego includes, se utiliza para saber la existencia de un elemento dentro de un arreglo.
let numbers = [1,2,4,7,8];

if (numbers.includes(10)) {
    console.log('Si se encuentra el valor 7');
} else {
    console.log('No se encuentra.');
}

// Eleva a la potencia

let base = 2;
let exponent = 3;
let result = base ** exponent;

console.log(result);